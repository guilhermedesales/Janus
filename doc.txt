*tarefas*

//crud//

- adicionar tarefa;                         (x)
- editar tarefa;                            (x)
- excluir tarefa;                           (x)

//status//

- atualizar status pra concluido;           (x)
- atualizar status pra atrasado;            (x)

//filtro//

- listar todas as tarefas;                  (x)
- listar tarefas por categoria;             (x)
- listar tarefas do dia;                    (x)
- listar tarefas da semana;                 (x)
- listar tarefas do mes;                    (x)
- listar tarefas atrasadas;                 (x)
- listar tarefas por prioridade(todos);     (x)

*categorias*

//crud//

- adicionar categoria;                      (x)
- editar categoria;                         (x)
- excluir categoria;                        (x)

//filtro//

- listar todos as categorias;               (x) //talvez seja inutil


acessar swagger: http://localhost:8080/swagger-ui.html

// arrumar //

- usar dto pra categoria
- arrumar auth de tarefas

--- v2 (refatoração geral do proj) ---

- criar endpoint de buscar tarefas por categoria usando o id da categoria


---- v3 (usando a api de auth pra lidar com a parte de autenticação) ---

